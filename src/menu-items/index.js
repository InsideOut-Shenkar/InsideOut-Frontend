// project import
import dashboard from './dashboard';
import patients from './patients';
import prediction from './prediction';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, patients, prediction]
};

export default menuItems;
